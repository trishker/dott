<h1>Messages Index</h1>

<%= button_to "Add New Message", new_message_path, method: :get %>
<ul>
  <% @messages.each do |message|%>
    <li><%= link_to message.message_title, message_path(message)%></li>
  <%end%>
